export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <div className="flex justify-center content-center items-center h-full w-full">
    <span className="loader"></span>
  </div>
}